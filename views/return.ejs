<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wardrobe</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Dancing+Script:wght@400;700&family=Montserrat:wght@400;700&family=Lora:wght@400;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <link rel="stylesheet" href="/css/style.css">
  
      
     
</head>

<body>
    <%- include('partials/navbar') %>
    <form method="GET" action="/product/return/returnsPage">
        <label>Filter By Date:</label>
        <select name="filter" onchange="this.form.submit()">
            <option value="">All</option>
            <option value="today" <%= filter === "today" ? "selected" : "" %>>Today</option>
            <option value="last_month" <%= filter === "last_month" ? "selected" : "" %>>Last 1 Month</option>
        </select>
    </form>
    <table border="1">
        <thead>
            <tr>
                <th>Order Number</th>
                <th>Customer</th>
                <th>Payment Method</th>
                <th>Order Date</th>
                <th>Return Requested</th>
                <th>Return Status</th>
                <th>Pickup Date</th>
            </tr>
        </thead>
        <tbody>
            <% orders.forEach(order => { %>
                <tr>
                    <td><%= order.orderNumber %></td>
                    <td><%= order.address.fullName %></td>
                    <td><%= order.paymentDetails.method %></td>
                    <td><%= new Date(order.orderDate).toLocaleDateString() %></td>
                    <td>
                        <%= order.return_cancel.cancelRequested ? "Yes" : "No" %>
                    </td>
                    <td>
                        <% if (order.return_cancel.returnInitiated) { %>
                            <span class="text-warning">Pending</span>
                        <% } else { %>
                            <span class="text-danger">Not Initiated</span>
                        <% } %>
                    </td>
                    <td>
                        <% if (order.return_cancel.returnInitiated) { %>
                            <%= new Date(new Date(order.updatedAt).getTime() + 5 * 24 * 60 * 60 * 1000).toLocaleDateString() %>
                        <% } else { %>
                            ---
                        <% } %>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
        